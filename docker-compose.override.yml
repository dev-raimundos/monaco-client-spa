services:
    monaco-client:
        image: node:20-alpine
        command: npm run start -- --host 0.0.0.0
        volumes:
            - .:/app
        working_dir: /app
        ports:
            - "4200:4200"
